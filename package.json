{
  "name": "knowledge-os",
  "version": "0.35.0",
  "private": true,
  "type": "module",
  "description": "Test + lint tooling for the knowledge-os plugin (zero runtime dependencies).",
  "scripts": {
    "test": "node --test",
    "lint": "node --check install.mjs && for f in scripts/*.mjs scripts/lib/*.mjs template/scripts/*.mjs; do node --check \"$f\" || exit 1; done"
  }
}
